# SWE & SVoD 

Minimal setup to run the SWE and SVoD CIFAR-100 experiment as described in https://arxiv.org/abs/2509.18842

## Install
```
pip install -r requirements.txt
```

## Run
```
python main.py --device cuda
```
Outputs are written to `extension/output/`.
